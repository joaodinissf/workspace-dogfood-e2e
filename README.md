# workspace-dogfood-e2e

Disposable synthetic fixture for end-to-end acceptance tests.

Run the dependency-free checks with `node --test` on Node.js 22 or later.
The implementation is in `greeting.js`; its tests are in `greeting.test.js`.

This repository contains no production code or credentials. Test pull requests
are temporary and are not intended to be merged.
